{% extends 'base.html.twig' %}

{% block body %}


  {% include("menus/navigation.html.twig") %}
    {% include("headers/header_adm.html.twig") %}

    <div class="container">
        <!-- Article main content -->
        <article class="col-md-12 maincontent">
            <div class="container">
                <div class="row">	
                    <h1>Liste des users </h1>

                    <hr style="width:100%; height:10px; background-color:#1B4F72 ">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>Nom</th>
                                    <th>Prenom</th>
                                    <th>Photo</th>
                                    <th>Date_naissance</th>
                                    <th>Login</th>
                                    <th>Password</th>
                                    <th>Adresse</th>
                                    <th>Email</th>
                                    <th>Role</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for user in users %}
                                    <tr>
                                        <td>{{ user.id }}</td>
                                        <td>{{ user.nom }}</td>
                                        <td>{{ user.prenom }}</td>
                                        <td>{{ user.photo }}</td>
                                        <td>{{ user.datenaissance | date('d/m/Y') }}</td>
                                        <td>{{ user.login }}</td>
                                        <td>{{ user.password }}</td>
                                        <td>{{ user.adresse }}</td>
                                        <td>{{ user.email }}</td>
                                        <td>{{ user.role }}</td>
                                        <td> 
                                            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                                                <div class="text-center">
                                                    <a href="#" class="btn btn-success"> affich / </a> 
                                                    <a href="#" class="btn btn-warning"> edit / </a> 
                                                    {# <a href="{{path('adm_user_delete', {'id': user.id})}}" class="btn btn-danger"> Suppr  </a> #} 
                                                     {# {{ include('user/_delete_form.html.twig') }} #}
                                                    
                                                    
                                                </div>
                                            </div> 
                    </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="5"> Aucun User pour lemoment  </td>
                                    </tr>
                                {% endfor %}
                        </tbody>
                    </table>
                        
                    <br><br><br><br>
                </div>
                <div>
                     <a href="{{path('app_user_new')}}" class="btn btn-primary"> Ajouter une user /  </a> <br><br><br><br> 
                </di>
            </div>
        </article>
    </div>
{% endblock %}