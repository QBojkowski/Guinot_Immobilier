{% extends 'base.html.twig' %}

{% block body %}


  {% include("menus/navigation.html.twig") %}
    {% include("headers/header_adm.html.twig") %}

    <div class="container">
        <!-- Article main content -->
        <article class="col-md-12 maincontent">
            <div class="container">
                <div class="row">	
                    <h1>Liste des locations </h1>

                    <hr style="width:100%; height:10px; background-color:#1B4F72 ">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>date</th>
                                    <th>Titre</th>
                                    <th>Description</th>
                                    <th>Surface</th>
                                    <th>Type</th>
                                    <th>Nb_chambre</th>
                                    <th>Nb_etage</th>
                                    <th>Prix</th>
                                    <th>Adresse</th>
                                    <th>Accessibilité</th>
                                    <th>Statut</th>
                                    <th>Alaune</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for location in locations %}
                                    <tr>
                                        <td>{{ location.id }}</td>
                                        <td>{{ location.date | date('d/m/Y') }}</td>
                                        <td>{{ location.titre }}</td>
                                        <td>{{ location.description }}</td>
                                        <td>{{ location.surface }}</td>
                                        <td>{{ location.type }}</td>
                                        <td>{{ location.nbchambre }}</td>
                                        <td>{{ location.nbetage }}</td>
                                        <td>{{ location.prix }}</td>
                                        <td>{{ location.adresse }}</td>
                                        <td>{{ location.accessibilite }}</td>
                                        <td>{{ location.statut }}</td>
                                        <td>{{ location.alaune }}</td>
                                        <td> 
                                            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                                                <div class="text-center">
                                                    <a href="#" class="btn btn-success"> affich / </a> 
                                                    <a href="#" class="btn btn-warning"> edit / </a> 
                                                    {# <a href="{{path('adm_location_delete', {'id': location.id})}}" class="btn btn-danger"> Suppr  </a> #} 
                                                     {# {{ include('location/_delete_form.html.twig') }} #}
                                                    
                                                    
                                                </div>
                                            </div> 
                    </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="5"> Aucun article pour lemoment  </td>
                                    </tr>
                                {% endfor %}
                        </tbody>
                    </table>
                        
                    <br><br><br><br>
                </div>
                <div>
                    <a href="{{path('app_location_new')}}" class="btn btn-primary"> Ajouter une location /  </a> <br><br><br><br>
                </di>
            </div>
        </article>
    </div>
{% endblock %}